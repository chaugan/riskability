# Where this forwarder sends data. Mounted into the UF as its own small app so
# the shipped TA stays exactly as an operator would receive it.
[tcpout]
defaultGroup = riskability_indexers

[tcpout:riskability_indexers]
server = riskability-splunk:9997

[tcpout-server://riskability-splunk:9997]
