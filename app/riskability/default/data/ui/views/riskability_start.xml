<dashboard version="1.1" theme="dark" stylesheet="application.css,riskability_start.css">
  <!-- The same test the "pipeline did not complete" alert makes, deliberately.
       This used to ask the checkpoint whether folded_run was set, which is a
       different question: a host that has simply not been re-matched lately has
       no acknowledgement either, and the banner accused the pipeline of being
       behind whenever the matcher had correctly skipped an unchanged host. The
       condition that matters is a receipt saying findings were produced with no
       state rows carrying that run. -->
  <search>
    <query>index=`riskability_index_findings` sourcetype=riskability:findings
    record_type="match_receipt"
    earliest=0 latest=+10y
    _index_earliest=`riskability_stuck_window` _index_latest=now
| stats max(match_run) AS match_run, sum(findings_produced) AS findings_produced BY hostname
| where findings_produced &gt; 0
| lookup riskability_matchstate_lookup hostname OUTPUT folded_run
| eval acknowledged = if(isnotnull(folded_run) AND abs(tonumber(folded_run) - match_run) &lt; 300, 1, 0)
| where acknowledged == 0
| stats count AS stale</query>
    <earliest>0</earliest>
    <latest>now</latest>
    <done>
      <condition match="$result.stale$ &gt; 0">
        <set token="pipeline_stale">1</set>
      </condition>
      <condition>
        <unset token="pipeline_stale"></unset>
      </condition>
    </done>
  </search>


  <label>Start here</label>
  <description>What this app does, what its numbers mean, and what they do not.</description>

  <row>
    <panel>
      <html>
        <div class="rk-start-lede">
          <h2>Riskability finds known vulnerabilities in the software your fleet actually has
              installed, using CVE databases you carry in by hand.</h2>
          <p>
            Built for search heads with no internet access. Nothing calls out: the vulnerability
            data is a file you build on a connected machine and import on
            <b>Feed administration</b>; inventory arrives from <b>swinv</b> through your
            forwarders.
          </p>
          <p class="rk-start-honest">
            <b>An empty panel means <em>nothing was found</em>, not that there is nothing to
            find.</b> A feed never imported, a host that stopped reporting, and a package no
            advisory covers all produce the same zero. Hence the three clocks below and the
            <b>Coverage</b> page.
          </p>
        </div>
      </html>
    </panel>
  </row>

  <row>
    <panel>
      <title>Feed age</title>
      <single>
        <search>
          <query>| inputlookup riskability_feedstate_lookup
| search _key=current
| eval days = round((now() - imported_at) / 86400, 1)
| fields days</query>
        </search>
        <option name="underLabel">days since the CVE data was imported</option>
        <option name="rangeColors">["0x53a051","0xf8be34","0xdc4e41"]</option>
        <option name="rangeValues">[7,30]</option>
        <option name="useColors">1</option>
        <option name="drilldown">none</option>
      </single>
    </panel>
    <panel>
      <title>Newest inventory</title>
      <single>
        <search>
          <query>| `riskability_hosts_seen`
| stats max(host_last_scan) AS t
| eval hours = round((now() - t) / 3600, 1)
| fields hours</query>
        </search>
        <option name="underLabel">hours since any host last reported</option>
        <option name="rangeColors">["0x53a051","0xf8be34","0xdc4e41"]</option>
        <option name="rangeValues">[6,48]</option>
        <option name="useColors">1</option>
        <option name="drilldown">none</option>
      </single>
    </panel>
    <panel>
      <title>Last match</title>
      <single>
        <search>
          <query>| inputlookup riskability_matchstate_lookup
| stats max(matched_at) AS t
| eval hours = round((now() - t) / 3600, 1)
| fields hours</query>
        </search>
        <option name="underLabel">hours since inventory was compared to the feed</option>
        <option name="rangeColors">["0x53a051","0xf8be34","0xdc4e41"]</option>
        <option name="rangeValues">[2,26]</option>
        <option name="useColors">1</option>
        <option name="drilldown">none</option>
      </single>
    </panel>
    <panel>
      <title>Hosts reporting</title>
      <single>
        <search>
          <query>| `riskability_hosts_seen` | stats count AS n</query>
        </search>
        <option name="underLabel">seen at least once</option>
        <option name="drilldown">none</option>
        <option name="useColors">0</option>
      </single>
    </panel>
  </row>

  <row>
    <panel>
      <html>
        <div class="rk-start-note">
          <b>Every number here is only as current as the oldest of those three.</b>
          A fresh scan against a six-month-old feed finds six-month-old vulnerabilities.
        </div>
      </html>
    </panel>
  </row>

  <row>
    <panel>
      <html>
        <div class="rk-status" style="margin:0">
          <b>A host with nothing new to report is not a host that stopped reporting.</b>
          <span>
            swinv sends a <b>heartbeat</b> every scan - a digest of the component list - and the
            full list only when that digest changes, so most scans are a few hundred bytes
            rather than a few megabytes and only changed hosts are re-matched.
          </span>
          <span>
            <b>Last inventory</b> above therefore tracks heartbeats, not full scans: it shows the
            host is still talking. Findings come from its last full component list, which may
            predate the last heartbeat and is still current, because the digest confirms nothing
            changed. Collectors that send no heartbeat still work; the app computes the digest
            itself.
          </span>
        </div>
      </html>
    </panel>
  </row>

  <row depends="$pipeline_stale$">
    <panel>
      <html>
        <div class="rk-status rk-bad">
          <b>Some hosts have been matched but their results have not been folded in.</b>
          <span>
            Open counts for those hosts are stale-high and closing is withheld until the fold
            completes, so risk may be over-reported, never under-reported. Usually clears within
            the hour; if not, the pipeline has stopped and
            <a href="riskability_admin">Admin</a> shows which job.
          </span>
        </div>
      </html>
      <viz type="riskability.riskability_grid">
        <search>
          <query>index=`riskability_index_findings` sourcetype=riskability:findings
    record_type="match_receipt"
    earliest=0 latest=+10y
    _index_earliest=`riskability_stuck_window` _index_latest=now
| stats max(match_run) AS match_run, sum(findings_produced) AS findings_produced BY hostname
| where findings_produced &gt; 0
| lookup riskability_matchstate_lookup hostname OUTPUT folded_run
| eval acknowledged = if(isnotnull(folded_run) AND abs(tonumber(folded_run) - match_run) &lt; 300, 1, 0)
| where acknowledged == 0
| eval matched = strftime(match_run, "%Y-%m-%d %H:%M:%S")
| eval folded = if(isnull(folded_run) OR folded_run="", "never",
                   strftime(tonumber(folded_run), "%Y-%m-%d %H:%M:%S"))
| eval waiting = tostring(round(now() - match_run), "duration")
| table hostname, matched, findings_produced, folded, waiting
| rename hostname AS "Host", matched AS "Matched at",
         findings_produced AS "Findings the matcher reported",
         folded AS "Last folded in", waiting AS "Waiting"</query>
          <earliest>0</earliest>
          <latest>now</latest>
        </search>
        <option name="riskability.riskability_grid.drilldown">disabled</option>
        <option name="height">160</option>
      </viz>
    </panel>
  </row>

  <row>
    <panel>
      <title>Open findings</title>
      <single>
        <search>
          <query>| inputlookup riskability_openstate_host_lookup | stats sum(findings) AS n</query>
        </search>
        <option name="underLabel">excluding accepted risks</option>
        <option name="rangeColors">["0x708794","0xf8be34","0xdc4e41"]</option>
        <option name="rangeValues">[1,100]</option>
        <option name="useColors">1</option>
        <option name="drilldown">none</option>
      </single>
    </panel>
    <panel>
      <title>Accepted risks</title>
      <single>
        <search>
          <query>| inputlookup riskability_openstate_host_lookup | stats sum(accepted) AS n</query>
        </search>
        <option name="underLabel">excluded on purpose, with a recorded reason</option>
        <option name="drilldown">none</option>
        <option name="useColors">0</option>
      </single>
    </panel>
    <panel>
      <title>Components tracked</title>
      <single>
        <search>
          <query>| inputlookup riskability_matchstate_lookup | stats sum(n_components) AS n</query>
        </search>
        <option name="underLabel">installed pieces of software seen</option>
        <option name="drilldown">none</option>
        <option name="useColors">0</option>
      </single>
    </panel>
  </row>

  <row>
    <panel>
      <title>How a finding is made</title>
      <html>
        <div class="rk-start-flow">
          <div class="rk-step">
            <div class="rk-step-n">1</div>
            <div>
              <b>swinv walks the machine</b>
              <p>Not just the package manager: also container images, snap bases and chroots.
                 It records the <em>path</em> of every copy, which separates a patched host
                 from one with a stale copy in an image.</p>
            </div>
          </div>
          <div class="rk-step">
            <div class="rk-step-n">2</div>
            <div>
              <b>Each component gets an identity</b>
              <p>A <b>PURL</b> when a package manager installed it (<code>pkg:deb/ubuntu/openssl</code>),
                 otherwise a <b>CPE</b>, which is most Windows software. PURLs come from
                 package records; CPEs are inferred from a display name, so Windows findings
                 are always low <b>confidence</b>.</p>
            </div>
          </div>
          <div class="rk-step">
            <div class="rk-step-n">3</div>
            <div>
              <b>Versions are compared using the right rules</b>
              <p>Debian, RPM, Alpine, npm, Python, Maven and Go each order versions
                 differently; text comparison gets it wrong. Where the comparison cannot be made
                 properly the finding is marked <b>low</b> rather than asserted.</p>
            </div>
          </div>
          <div class="rk-step">
            <div class="rk-step-n">4</div>
            <div>
              <b>The finding is tracked over time</b>
              <p>A finding that stops appearing is not automatically fixed. Newer version at
                 the same path: <b>mitigated</b>. Gone: <b>removed</b>. Host stopped reporting:
                 <b>unknown</b>. Silence is never success.</p>
            </div>
          </div>
          <div class="rk-step">
            <div class="rk-step-n">5</div>
            <div>
              <b>Reach decides the order of the work</b>
              <p>The collector also records what is <em>listening</em>, which process holds the
                 socket, and which containers run behind it. On this fleet that separates a few
                 reachable findings from tens of thousands that are not; see <b>Exposure</b>.
                 Reach re-orders the work, never shortens it: an unreachable vulnerability is
                 still a vulnerability, and a host whose collector is too old to report ports is
                 shown as <b>not assessed</b>, not safe.</p>
            </div>
          </div>
        </div>
      </html>
    </panel>
  </row>

  <row>
    <panel>
      <title>How MITRE ATT&amp;CK fits, and where it loses information</title>
      <html>
        <div class="rk-funnel">
          <div class="rk-funnel-row">
            <div class="rk-node">
              <div class="rk-node-t">CVE</div>
              <div class="rk-node-s">a specific vulnerability<br/>in a specific product</div>
            </div>
            <div class="rk-hop">
              <div class="rk-hop-arrow">&#8594;</div>
              <div class="rk-hop-l">most CVEs have none</div>
            </div>
            <div class="rk-node">
              <div class="rk-node-t">CWE</div>
              <div class="rk-node-s">the <em>class</em> of mistake<br/>e.g. use-after-free</div>
            </div>
            <div class="rk-hop">
              <div class="rk-hop-arrow">&#8594;</div>
              <div class="rk-hop-l">many-to-many</div>
            </div>
            <div class="rk-node">
              <div class="rk-node-t">CAPEC</div>
              <div class="rk-node-s">a pattern of attack<br/>that abuses it</div>
            </div>
            <div class="rk-hop">
              <div class="rk-hop-arrow">&#8594;</div>
              <div class="rk-hop-l">many-to-many</div>
            </div>
            <div class="rk-node rk-node-end">
              <div class="rk-node-t">ATT&amp;CK</div>
              <div class="rk-node-s">what an adversary<br/>could then do</div>
            </div>
          </div>
          <p class="rk-funnel-warn">
            <b>Every hop is a mapping MITRE publishes, and every hop loses information.</b>
            A CVE with no CWE never reaches the matrix, so a technique missing from the
            ATT&amp;CK page is not one you are safe from. One weakness maps to several
            techniques, so a vulnerability is drawn several times: the widths never add up to a
            total.
          </p>
          <p class="rk-funnel-warn">
            The chain says <em>&#8220;this class of weakness enables that technique&#8221;</em>. It
            does not say an adversary used it, or that it is exploitable in your environment. Use
            the <b>KEV</b> and <b>EPSS</b> columns on <b>Findings</b> for that: real-world
            exploitation rather than classification.
          </p>
        </div>
      </html>
    </panel>
  </row>

  <row>
    <panel>
      <title>How much survives the chain, right now</title>
      <viz type="riskability.riskability_chart">
        <search>
          <query>| `riskability_open_findings`
| stats dc(cve_id) AS n
| eval stage = "1. open CVEs"
| append
    [| `riskability_finding_cwes` | stats dc(cve_id) AS n | eval stage = "2. have a CWE recorded"]
| append
    [| `riskability_attack_surface` | stats dc(cve_id) AS n | eval stage = "3. reach a technique"]
| eval series = stage
| fields stage, series, n</query>
        </search>
        <option name="riskability.riskability_chart.chartType">stackedbar</option>
        <option name="riskability.riskability_chart.valueLabel">CVEs</option>
        <option name="riskability.riskability_chart.emptyText">No open findings to trace through the chain.</option>
        <!-- Illustrative rather than navigational: a click here has nowhere
             useful to go, and Splunk's default for a drilldown with no target
             is to abandon the dashboard for a raw search. -->
        <option name="riskability.riskability_chart.drilldown">disabled</option>
        <option name="height">280</option>
      </viz>
      <html>
        <div class="rk-start-note">
          The drop between these bars is what the mapping costs, not a defect. Read the
          ATT&amp;CK page as <em>&#8220;capabilities our open findings could hand an
          attacker&#8221;</em>, never as a complete picture of exposure.
        </div>
      </html>
    </panel>
  </row>

  <row>
    <panel>
      <title>Where to go</title>
      <html>
        <div class="rk-start-guide">
          <div class="rk-guide-item"><b>Fleet overview</b>
            <span>The numbers, and how stale they are.</span></div>
          <div class="rk-guide-item"><b>Findings</b>
            <span>The working list: what to patch, ranked by likelihood of exploitation rather
              than severity.</span></div>
          <div class="rk-guide-item"><b>Remediation</b>
            <span>What was fixed, with the version change proving it, and what merely stopped
              being reported.</span></div>
          <div class="rk-guide-item"><b>MITRE ATT&amp;CK</b>
            <span>Lossy classification of what open findings could enable. Context, not
              evidence.</span></div>
          <div class="rk-guide-item"><b>Exposure</b>
            <span>Which findings the network can reach, and which sit inside containers.
              Re-orders the work, never removes any of it.</span></div>
          <div class="rk-guide-item"><b>Hosts</b>
            <span>One machine at a time, split by filesystem root.</span></div>
          <div class="rk-guide-item"><b>Coverage</b>
            <span>What the app <em>cannot</em> see: ecosystems in the fleet the imported feed
              knows nothing about.</span></div>
          <div class="rk-guide-item"><b>CVE encyclopaedia</b>
            <span>What a vulnerability actually is, offline. Every CVE id in every table on
              every page opens it here, so you never have to leave an air-gapped room to look
              one up.</span></div>
          <div class="rk-guide-item"><b>Risk exceptions</b>
            <span>Findings handled another way, and how. Excluded from every risk number and
              counted separately, so they are never hidden.</span></div>
          <div class="rk-guide-item"><b>Feed administration</b>
            <span>Import the offline CVE data, and download the builder that makes it.</span></div>
        </div>
      </html>
    </panel>
  </row>

  <row>
    <panel>
      <title>Words this app uses</title>
      <html>
        <div class="rk-glossary">
          <dl>
            <dt>CVE</dt><dd>One vulnerability in one product.</dd>
            <dt>CWE</dt><dd>The <em>class</em> of mistake. Most CVEs never get one assigned.</dd>
            <dt>CAPEC</dt><dd>An attack pattern abusing such a weakness.</dd>
            <dt>ATT&amp;CK</dt><dd>Adversary behaviour by tactic. Reached here through CWE and
              CAPEC, so lossily.</dd>
            <dt>PURL</dt><dd>Identity read from package manager records. Precise.</dd>
            <dt>CPE</dt><dd>Identity inferred where no package manager was involved. Weaker
              evidence.</dd>
            <dt>EPSS</dt><dd>Published estimate of exploitation within 30 days. Better for
              prioritising than severity.</dd>
            <dt>KEV</dt><dd>CISA's exploited-in-the-wild list. The strongest signal available
              offline.</dd>
            <dt>scope / root</dt><dd>Filesystem the component was found in: host, snap base,
              container image, chroot.</dd>
            <dt>confidence</dt><dd>Strength of the evidence, not severity. <em>high</em>:
              identity and version both from package records. <em>informational</em>: an
              upstream version range covering a distribution-maintained package, which the
              vendor may have patched by backport. <em>low</em>: identity inferred from a
              generated CPE, or the version could not be compared properly.</dd>
            <dt>authority</dt><dd>Who says the version is affected: distribution, upstream
              ecosystem, or NVD. Distributions win, because they backport.</dd>
            <dt>generation</dt><dd>One complete feed import. Findings match a single generation,
              never a half-written one.</dd>
          </dl>
        </div>
      </html>
    </panel>
  </row>
</dashboard>
