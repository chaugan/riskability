<form version="1.1" theme="dark" script="riskability_exceptions.js" stylesheet="riskability_exceptions.css">
  <label>Findings</label>
  <description>Every vulnerability the current feed can attribute to installed software.</description>
  <fieldset submitButton="false" autoRun="true">
    <!-- The choices below are injected into SPL unquoted, which is safe because
         they are a closed set defined here, not user input. Passing them
         through |s would quote the whole expression into a literal string and
         the filter would match nothing at all. -->
    <input type="dropdown" token="confidence_tok" searchWhenChanged="true">
      <label>Confidence</label>
      <choice value="confidence=high">High only</choice>
      <choice value="confidence=*">All</choice>
      <choice value="confidence=low">Low only</choice>
      <choice value="confidence=informational">Informational only</choice>
      <default>confidence=high</default>
      <initialValue>confidence=high</initialValue>
    </input>
    <input type="dropdown" token="scope_tok" searchWhenChanged="true">
      <label>Filesystem root</label>
      <choice value="scope=*">Every root</choice>
      <choice value="scope=host">Host packages only</choice>
      <choice value="scope=snap">Snap bases only</choice>
      <choice value="scope=container OR scope=&quot;nested-root&quot;">Containers and nested roots</choice>
      <default>scope=*</default>
      <initialValue>scope=*</initialValue>
    </input>
    <input type="text" token="host_tok" searchWhenChanged="true">
      <label>Host (wildcards allowed)</label>
      <default>*</default>
      <initialValue>*</initialValue>
    </input>
  </fieldset>

  <row>
    <panel>
      <html>
        <div id="rk-exc-toolbar" class="rk-exc-toolbar">
          <span class="rk-exc-hint">Select rows to accept a risk. Every selected row must share
            one CVE &#8212; one justification cannot honestly cover several different
            vulnerabilities.</span>
          <span id="rk-exc-summary" class="rk-exc-summary"></span>
          <button id="rk-exc-accept" class="rk-exc-btn" type="button" disabled="disabled">
            Accept risk&#8230;</button>
        </div>
      </html>
    </panel>
  </row>

  <row>
    <panel>
      <title>Findings, ranked by exploitation likelihood (EPSS) rather than CVSS</title>
      <viz type="riskability.riskability_grid">
        <search>
          <query>| `riskability_open_findings_all`
| search hostname=$host_tok|s$
| search $confidence_tok$ $scope_tok$
| eval installed_version = last_version
| eval epss_pct = if(isnotnull(epss), round(epss*100, 2), null())
| eval kev = if(kev_added!="", "yes", "")
| eval accepted_label = if(accepted=="1", "accepted", "")
| table cve_id, package, installed_version, fixed_version, hostname, scope_id, ecosystem, match_authority, confidence, epss_pct, kev, accepted_label, title, finding_key
| rename cve_id AS "CVE", package AS "Package", installed_version AS "Installed",
         fixed_version AS "Fixed in", hostname AS "Host", scope_id AS "Root",
         ecosystem AS "Type", match_authority AS "Authority", confidence AS "Confidence",
         epss_pct AS "EPSS %", kev AS "KEV", accepted_label AS "Accepted",
         title AS "Summary", finding_key AS "Key"
| sort - "EPSS %"</query>
        </search>
        <option name="riskability.riskability_grid.selectable">yes</option>
        <option name="riskability.riskability_grid.hideColumns">Key</option>
        <option name="riskability.riskability_grid.emptyText">No findings matched. With the Confidence filter on "High only" this can mean the evidence was never strong enough to assert one, not that the fleet is clean — switch it to All to see what was found and why it was downgraded.</option>
      </viz>
      <html>
        <div class="rk-status rk-warn" style="margin:8px 0 0">
          <b>The column filters narrow what is shown, not what was searched.</b>
          <span>
            Typing in a column header filters the rows already loaded into the page. It does
            not re-run the search, so it cannot reach findings the search did not return, and
            it does not change the panels on any other dashboard. For anything you intend to
            act on, narrow it with the Confidence, Root and Host inputs above, which filter in
            the search itself. "Download shown rows" likewise exports what is currently
            displayed and filtered, not the whole result set.
          </span>
        </div>
      </html>
    </panel>
  </row>

  <row>
    <panel>
      <title>Why a finding is only "informational"</title>
      <html>
        <p>Confidence is about the strength of the evidence, not the severity of the vulnerability.</p>
        <ul>
          <li><b>high</b> &#8212; a distro or ecosystem advisory names this package and release, and both
              versions parsed cleanly under that ecosystem's rules.</li>
          <li><b>informational</b> &#8212; an upstream version range covers a distribution package.
              Ubuntu and Red Hat backport fixes without changing the upstream version, so
              <code>openssl 3.0.13-0ubuntu3.4</code> may well already be patched against a CVE that
              upstream fixed in 3.0.14. Shown so it is not hidden; not asserted, because it is not known.</li>
          <li><b>low</b> &#8212; the comparison could not be made properly: no comparator for the
              ecosystem, an unparseable version such as <code>UNKNOWN</code>, or an identity inferred
              from a binary rather than a package record.</li>
        </ul>
      </html>
    </panel>
  </row>
</form>
