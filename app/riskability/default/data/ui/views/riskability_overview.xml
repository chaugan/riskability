<dashboard version="1.1" theme="dark">
  <label>Fleet overview</label>
  <description>Vulnerability posture across every host reporting a swinv inventory.</description>

  <!-- An air-gapped feed is only as good as the last upload, so how stale it
       is comes first, before any number derived from it. -->
  <row>
    <panel>
      <title>Vulnerability feed</title>
      <single>
        <search>
          <query>| inputlookup riskability_feedstate_lookup
| where isnotnull(imported_at)
| eval age_days = round((now() - imported_at)/86400, 1)
| fields age_days</query>
        </search>
        <option name="drilldown">none</option>
        <option name="underLabel">days since feed was imported</option>
        <option name="rangeColors">["0x53a051","0xf8be34","0xdc4e41"]</option>
        <option name="rangeValues">[7,30]</option>
        <option name="useColors">1</option>
      </single>
    </panel>
    <panel>
      <title>Hosts reporting</title>
      <single>
        <search>
          <query>index=riskability_inventory sourcetype=riskability:swinv earliest=-7d
| stats dc(hostname) AS hosts</query>
        </search>
        <option name="drilldown">none</option>
        <option name="underLabel">in the last 7 days</option>
      </single>
    </panel>
    <panel>
      <title>Components tracked</title>
      <single>
        <search>
          <query>`riskability_latest_inventory`
| stats count AS components</query>
        </search>
        <option name="drilldown">none</option>
        <option name="underLabel">latest state, all hosts</option>
      </single>
    </panel>
  </row>

  <row>
    <panel>
      <title>Open findings</title>
      <single>
        <search>
          <query>| `riskability_open_findings` | stats dc(finding_key) AS n</query>
        </search>
        <option name="underLabel">excluding accepted risks</option>
        <option name="rangeColors">["0x53a051","0xf8be34","0xdc4e41"]</option>
        <option name="rangeValues">[1,100]</option>
        <option name="useColors">1</option>
        <option name="drilldown">none</option>
      </single>
    </panel>
    <panel>
      <title>Accepted risks</title>
      <single>
        <search>
          <query>| `riskability_accepted_findings` | stats dc(finding_key) AS n</query>
        </search>
        <option name="underLabel">excluded from every number above</option>
        <option name="drilldown">none</option>
        <option name="useColors">0</option>
      </single>
    </panel>
    <panel>
      <title>Accepted and known-exploited</title>
      <single>
        <search>
          <query>| `riskability_accepted_findings` | where isnotnull(kev_added) AND kev_added!="" | stats dc(cve_id) AS n</query>
        </search>
        <option name="underLabel">on the CISA KEV list, and excepted</option>
        <option name="rangeColors">["0x53a051","0xdc4e41"]</option>
        <option name="rangeValues">[0]</option>
        <option name="useColors">1</option>
        <option name="drilldown">none</option>
      </single>
    </panel>
  </row>

  <row>
    <panel>
      <html>
        <div class="rk-status rk-warn" style="margin:0">
          <b>Accepted risks are excluded from every other number on this page.</b>
          <span>
            An accepted risk is one somebody has looked at and decided to handle another way,
            recorded with what that other way is. It is still a vulnerability, and it is still
            present. A fleet showing no open findings and four hundred accepted ones is not a
            clean fleet, which is why the count sits here rather than being hidden. Manage them
            on <b>Risk exceptions</b>.
          </span>
        </div>
      </html>
    </panel>
  </row>

  <row>
    <panel>
      <title>Where the open findings are, by package</title>
      <viz type="riskability.riskability_chart">
        <search>
          <query>| `riskability_open_findings`
| stats dc(cve_id) AS cves BY package
| sort - cves
| head 60</query>
        </search>
        <option name="riskability.riskability_chart.chartType">treemap</option>
        <option name="riskability.riskability_chart.valueLabel">CVEs</option>
        <option name="riskability.riskability_chart.emptyText">No open findings. If no feed has been imported this means the app cannot assess anything yet, not that the fleet is clean — check the Feed administration page.</option>
        <option name="height">420</option>
      </viz>
      <html>
        <div class="rk-status" style="margin:8px 0 0">
          <b>Area is distinct CVEs, not severity.</b>
          <span>
            A large tile is a package carrying many open CVEs, so it is where patching
            effort goes furthest. That is not the same as where the single worst
            vulnerability is — for that, sort the Findings page by EPSS.
          </span>
        </div>
      </html>
    </panel>
  </row>

  <row>
    <panel>
      <title>Findings by confidence</title>
      <viz type="riskability.riskability_chart">
        <search>
          <query>| `riskability_open_findings`
| stats count AS findings BY confidence
| eval sev = confidence
| sort confidence
| fields confidence, sev, findings</query>
        </search>
        <option name="riskability.riskability_chart.chartType">stackedbar</option>
        <option name="riskability.riskability_chart.valueLabel">findings</option>
        <option name="riskability.riskability_chart.emptyText">Nothing matched. With no feed imported the matcher has nothing to compare against.</option>
        <option name="height">300</option>
      </viz>
      <html>
        <div class="rk-status" style="margin:8px 0 0">
          <b>Confidence is ranked, not categorical.</b>
          <span>
            High means the package identity and version were both read from a package
            record. Low means the identity was inferred — every Windows finding lands
            here. Informational means the version could not be compared properly. Only
            the high band should drive patching decisions.
          </span>
        </div>
      </html>
    </panel>
    <panel>
      <title>Most affected hosts</title>
      <viz type="riskability.riskability_grid">
        <search>
          <query>| `riskability_open_findings`
| where confidence!="informational"
| stats dc(cve_id) AS cves, dc(package) AS packages BY hostname
| sort - cves
| head 20</query>
        </search>
        <drilldown>
          <link target="_blank">/app/riskability/riskability_hosts?form.hostname=$row.hostname$</link>
        </drilldown>
        <option name="drilldown">all</option>
        <option name="riskability.riskability_grid.emptyText">No host has an open finding. If no inventory has arrived, that is silence rather than a clean fleet.</option>
      </viz>
    </panel>
  </row>

  <row>
    <panel>
      <title>Known-exploited vulnerabilities present in the fleet (CISA KEV)</title>
      <viz type="riskability.riskability_grid">
        <search>
          <query>| `riskability_open_findings`
| where confidence!="informational"
| where isnotnull(kev_added) AND kev_added!=""
| stats dc(hostname) AS hosts, values(package) AS packages BY cve_id, kev_added, kev_due, epss
| sort - hosts</query>
        </search>
        <option name="riskability.riskability_grid.drilldown">disabled</option>
        <option name="riskability.riskability_grid.emptyText">No known-exploited CVEs matched. This panel is only as good as the KEV catalogue in the imported bundle — if KEV was unreachable when the bundle was built, it is blank for that reason.</option>
      </viz>
    </panel>
  </row>
</dashboard>
