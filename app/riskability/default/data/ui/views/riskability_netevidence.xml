<form version="1.1" theme="dark" script="riskability_help.js" stylesheet="application.css">
  <label>Network evidence</label>
  <description>Whether a permitted flow to a listening port has ever actually been observed, and where the app refuses to say.</description>

  <!-- Drilldown and filter state, set here rather than with hidden inputs.
       Splunk 9 renders an <input type="hidden"> as an ordinary labelled text
       box, which would put this page's internal filters in the filter bar as
       editable fields. Every filter needs an "everything" value because a
       search token cannot be conditional: the panels are malformed until the
       token exists at all. -->
  <init>
    <set token="grade_filter">observed_grade=*</set>
    <set token="grade_label"></set>
  </init>

  <row>
    <panel>
      <html>
        <div class="rk-status rk-warn" style="margin:0 0 4px;">
          <b>Silence here is not safety.</b>
          <span>A firewall logs a flow that happened. A permitted rule nobody has
          used yet produces no log line at all, so this evidence under-reports by
          construction: a port that reads <i>not observed</i> may simply be one
          nobody has tried. This page never lowers a finding's priority, and
          missing firewall data costs a finding confidence, never its place in
          the queue.</span>
        </div>
      </html>
    </panel>
  </row>

  <fieldset submitButton="false">
    <input type="dropdown" token="grade_filter" searchWhenChanged="true">
      <label>Grade</label>
      <choice value="observed_grade=*">All grades</choice>
      <choice value="observed_grade=&quot;confirmed observed&quot;">Confirmed observed</choice>
      <choice value="observed_grade=&quot;historically observed&quot;">Historically observed</choice>
      <choice value="observed_grade=&quot;not observed&quot;">Not observed</choice>
      <choice value="observed_grade=&quot;unknown&quot;">Unknown</choice>
      <default>observed_grade=*</default>
      <change>
        <set token="grade_label">$label$</set>
      </change>
    </input>
    <input type="text" token="host_filter" searchWhenChanged="true">
      <label>Host (blank for all)</label>
      <default>*</default>
      <initialValue>*</initialValue>
    </input>
  </fieldset>

  <row>
    <panel>
      <title>What the firewall evidence says about the fleet</title>
      <viz type="riskability.riskability_chart">
        <search>
          <!-- Read from the ledger the assess job wrote, never recomputed here.
               Exactly one place in the app decides a grade. -->
          <query>| inputlookup riskability_netevidence_lookup
| search hostname=$host_filter|s$
| stats count BY observed_grade
| eval rk_order = case(observed_grade=="confirmed observed", 0,
    observed_grade=="historically observed", 1,
    observed_grade=="not observed", 2, 1=1, 3)
| sort rk_order
| fields observed_grade, count</query>
          <earliest>0</earliest>
          <latest>now</latest>
        </search>
        <!-- A donut, not a column chart. There are at most four grades, which
             is the small ordered category set the donut is for, and the
             question this panel answers is "how much of the fleet is in each
             grade" - a share of a whole, which is what an arc reads as and a
             column does not. It also puts the grade names in a legend down the
             side instead of rotated under an axis, where two-word labels were
             being clipped to "-erved" and "-nown". -->
        <option name="riskability.riskability_chart.chartType">donut</option>
        <option name="riskability.riskability_chart.colorScale">category</option>
        <option name="riskability.riskability_chart.valueLabel">host and port rows</option>
        <option name="riskability.riskability_chart.emptyText">The assess job has not run, or it found no open finding on a listening port. Nothing here is a statement about your network.</option>
        <!-- Tall enough for the rotated category labels. The grade names are
             two words and the axis rotates them 40 degrees, so at the default
             height the container clipped them to their last few characters and
             the chart read "-erved" and "-nown". -->
        <option name="height">300</option>
        <drilldown>
          <set token="grade_filter">observed_grade="$row.observed_grade$"</set>
          <set token="grade_label">$row.observed_grade$</set>
        </drilldown>
      </viz>
    </panel>

    <panel>
      <title>Can this evidence say anything at all?</title>
      <viz type="riskability.riskability_grid">
        <search>
          <!-- coverage_state is the gate between "unknown" and any other
               answer, so it is shown as its own count rather than left to be
               inferred from the grades. A fleet that is entirely "no firewall
               data" is a configuration fact, not a finding. -->
          <query>| inputlookup riskability_netevidence_lookup
| search hostname=$host_filter|s$
| stats count AS "Host and port rows", dc(hostname) AS Hosts BY coverage_state
| rename coverage_state AS "Coverage state"
| eval "What it means" = case(
    'Coverage state'=="no firewall data", "No edges reached the app. Every row is unknown, and correctly so.",
    'Coverage state'=="no entry point declared", "Edges exist, but nothing declares where an attacker starts, so no absence means anything.",
    'Coverage state'=="firewall data stale", "The newest edge is older than the freshness window, so silence is about the feed, not the network.",
    'Coverage state'=="host not in graph", "No edge resolved to this host, so the app cannot claim it is visible to the firewall.",
    'Coverage state'=="covered", "The host resolved to at least one edge, so a silent port is evidence about the port.",
    1=1, "")
| table "Coverage state", "Host and port rows", Hosts, "What it means"</query>
          <earliest>0</earliest>
          <latest>now</latest>
        </search>
        <option name="riskability.riskability_grid.drilldown">disabled</option>
        <option name="riskability.riskability_grid.wrap">yes</option>
      
        <option name="riskability.riskability_grid.fitContent">Coverage state,Host and port rows,Hosts</option>
        <option name="riskability.riskability_grid.emptyText">The assess job has not run yet.</option>
        <option name="height">260</option>
      </viz>
    </panel>
  </row>

  <row>
    <panel>
      <title>The ledger $grade_label$</title>
      <viz type="riskability.riskability_grid">
        <search>
          <query>| inputlookup riskability_netevidence_lookup
| search hostname=$host_filter|s$ $grade_filter$
| eval rk_order = case(observed_grade=="confirmed observed", 0,
    observed_grade=="historically observed", 1,
    observed_grade=="not observed", 2, 1=1, 3)
| eval "Last edge" = if(isnotnull(edge_last_seen) AND edge_last_seen!="",
    strftime(tonumber(edge_last_seen), "%Y-%m-%d %H:%M"), "never")
| eval Sources = if(n_srcs > 0, srcs_text . " (" . n_srcs . ")", "")
| sort rk_order, hostname, port
| rename hostname AS Host, port AS Port, protocols_text AS Protocol,
    observed_grade AS Grade, observed_reason AS Why, entry_text AS "Entry point",
    entry_pressure AS Pressure, sessions AS Sessions
| table Host, Port, Protocol, Grade, "Entry point", Pressure, Sources, Sessions,
    "Last edge", Why</query>
          <earliest>0</earliest>
          <latest>now</latest>
        </search>
        <option name="riskability.riskability_grid.drilldown">disabled</option>
        <option name="riskability.riskability_grid.wrap">yes</option>
      
        <option name="riskability.riskability_grid.fitContent">Host,Port,Protocol,Grade,Pressure,Sessions,Last edge</option>
        <option name="riskability.riskability_grid.filterNote">$grade_label$</option>
        <option name="riskability.riskability_grid.emptyText">No row matches this grade and host.</option>
        <option name="height">460</option>
      </viz>
    </panel>
  </row>

  <row>
    <panel>
      <title>Where the app refused to name a host</title>
      <html>
        <div class="rk-status rk-warn" style="margin:0 0 6px;">
          <b>An address is only a host's for as long as something observed it holding one.</b>
          <span>These rows had a permitted flow to an address the inventory could not
          tie to exactly one machine at the moment it was logged, so the flow is
          reported and deliberately not attributed. Refusing is the safe wrong
          answer: a false attribution invents an attack path that nobody can find
          and everybody has to chase.</span>
        </div>
      </html>
      <viz type="riskability.riskability_grid">
        <search>
          <query>| inputlookup riskability_netevidence_lookup
| search hostname=$host_filter|s$
| where identity_state!="resolved"
| stats count AS Rows, values(hostname) AS Hosts BY identity_state
| eval "What it means" = case(
    identity_state=="shared", "More than one host has held this address, so a flow to it belongs to no one host.",
    identity_state=="stale", "The address claim does not cover the moment the flow was logged.",
    identity_state=="outside estate", "No host in the inventory has ever been seen holding this address. Expected for an internet entry point.",
    identity_state=="malformed address", "The firewall record's destination could not be parsed as an address.",
    identity_state=="no edge to resolve", "No permitted flow reached this host and port, so there was nothing to attribute.",
    1=1, "")
| eval Hosts = mvjoin(mvdedup(Hosts), " ")
| rename identity_state AS "Identity"
| sort - Rows
| table Identity, Rows, "What it means", Hosts</query>
          <earliest>0</earliest>
          <latest>now</latest>
        </search>
        <option name="riskability.riskability_grid.drilldown">disabled</option>
        <option name="riskability.riskability_grid.wrap">yes</option>
      
        <option name="riskability.riskability_grid.fitContent">Identity,Rows</option>
        <option name="riskability.riskability_grid.emptyText">Every edge that reached a host resolved to exactly one. Nothing was refused.</option>
        <option name="height">260</option>
      </viz>
    </panel>
  </row>

  <row>
    <panel>
      <title>Address coverage, and whether the collector could report it</title>
      <html>
        <div class="rk-status rk-warn" style="margin:0 0 6px;">
          <b>No row for a host means never assessed, not "holds no addresses".</b>
          <span>A host showing zero addresses may sit on a segment nothing logs, or
          every one of its listeners may bind a wildcard, which the bind-derived
          source cannot see at all. Those look identical here and are completely
          different facts, and only the second is a limit of this app rather than
          of the site. The collector and schema columns answer the "could it even
          have reported them" half.</span>
        </div>
      </html>
      <viz type="riskability.riskability_grid">
        <search>
          <query>| inputlookup riskability_hostaddr_hoststate_lookup
| search hostname=$host_filter|s$
| eval "Identity stable" = if(contested_addresses > 0,
    "no, " . contested_addresses . " address(es) claimed by another host", "yes")
| rename hostname AS Host, addresses AS Addresses, ipv4_addresses AS IPv4,
    ipv6_addresses AS IPv6, addr_source AS Source, swinv_version AS "Collector",
    schema_version AS Schema
| sort - Addresses, Host
| table Host, Addresses, IPv4, IPv6, "Identity stable", Source, Collector, Schema</query>
          <earliest>0</earliest>
          <latest>now</latest>
        </search>
        <option name="riskability.riskability_grid.drilldown">disabled</option>
        <option name="riskability.riskability_grid.wrap">yes</option>
      
        <option name="riskability.riskability_grid.fitContent">Host,Addresses,IPv4,IPv6,Source,Collector,Schema</option>
        <option name="riskability.riskability_grid.emptyText">No host has been assessed for addresses yet. The address snapshot job runs hourly.</option>
        <option name="height">300</option>
      </viz>
    </panel>
  </row>

  <row>
    <panel>
      <title>What to do with a grade</title>
      <html>
        <div class="rk-status" style="margin:0;">
          <b>Read a grade as a question about the finding, not a verdict on the host.</b>
          <span><i>Confirmed observed</i> means a permitted flow from a declared
          entry point reached this address and port recently: treat the finding
          on that port as reachable from where an attacker starts, and check
          the source addresses listed against what you expect to see there.
          <i>Historically observed</i> is the same evidence, older than the
          freshness window: the path existed, and may still.</span>
          <span><i>Not observed</i> means the firewall was watching this host and
          logged no permitted flow to this port from any entry point. That is
          evidence about the port and it is weaker than it looks: a permitted
          rule nobody has used yet produces no log line at all. Use it to order
          work, never to accept a risk.</span>
          <span><i>Unknown</i> means this page cannot say, and the reason column
          says why: no firewall data has been onboarded, no entry point has been
          declared, the data is stale, or the host never appeared in it. None of
          those is a statement about the network. If every row on this page
          reads unknown, the firewall feed has not been connected to Riskability
          yet; that is set up by an administrator in Riskability Configuration,
          and the details are in the app's documentation under network evidence.</span>
        </div>
      </html>
    </panel>
  </row>
</form>
