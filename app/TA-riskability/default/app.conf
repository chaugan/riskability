# Riskability Add-on: deploy to universal forwarders (inputs) and to indexers
# (index-time parsing). The search-head app "riskability" does the correlation.
[install]
is_configured = 1
state = enabled
build = 1

[package]
id = TA-riskability

[ui]
is_visible = 0
label = Riskability Add-on

[launcher]
author = chaugan
description = Collects swinv software inventory for the Riskability app.
version = 0.1.0
